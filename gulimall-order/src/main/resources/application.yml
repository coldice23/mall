spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: hanbing0223
    url: jdbc:mysql://localhost:3306/gulimall_oms?serverTimezone=Asia/Shanghai

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto

server:
  port: 9000